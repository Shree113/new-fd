{
  "rewrites": [
    { "source": "/api/*", "destination": "https://your-backend-url.onrender.com/api/:path*" }
  ],
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ]
}
